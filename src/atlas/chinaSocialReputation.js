// Public social-platform scoring is paused pending provenance, privacy, entity
// resolution, representativeness, and product review. Local research output is
// never imported into the production bundle.
export const SOCIAL_REPUTATION_META = Object.freeze({
  platforms: [],
  updatedAt: null,
  noteCount: 0,
  commentCount: 0,
  status: 'research-only'
});

export const SOCIAL_REPUTATION = Object.freeze({});
